{
  "name": "polyglot-telemetry-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — anonymous telemetry receiver for poly-glot-ai-cli",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "tail":   "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
